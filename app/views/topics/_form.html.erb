<h2>Add a New Topic:</h2>

<%= form_for Topic.new do |f| %>
  <div class='field'>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
